One-line: the standard text field used across the Diagnóstico form.

```jsx
<Input placeholder="seu@email.com" icon="mail" type="email" />
```

Focus is a green 3px ring, never blue. Pair with `Field` for labels and errors.
