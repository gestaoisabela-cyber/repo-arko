One-line: circular icon-only control for close, play, nav arrows and toolbar actions.

```jsx
<IconButton icon="x" label="Fechar" />
<IconButton icon="play" label="Assistir depoimento" variant="accent" size="lg" />
```

Variants: quiet (default), solid (navy), accent (green), outline. Always pass `label`.
