One-line: pill for filters, specialty/objective selection and removable choices in the diagnóstico form.

```jsx
<Tag variant="selected" icon="check" onClick={pick}>Aposentadoria</Tag>
```

Sentence case, not uppercase (that's Badge). Use `onClick` for selectable sets, `onRemove` for chips.
