One-line: renders the real Arko lockup from `assets/` — never redraw or recolor it.

```jsx
<Logo height={30} />
<Logo variant="mark" height={48} />
```

The horizontal PNG has a light background baked in, so place it on white/very light surfaces; on navy use `variant="mark"` or the brand name set in Poppins. Only two files exist (raster PNG) — request SVG masters from the brand owner.
