One-line: tab strip for switching content in place (vídeo / WhatsApp / NPS on the depoimentos page).

```jsx
<Tabs items={['Vídeo', 'WhatsApp', 'NPS']} value={tab} onChange={setTab} />
```

Active tab is navy text with a 2px green underline; pill variant sits on a pale navy track.
